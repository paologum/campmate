import { SafeAreaView } from "react-native";
import { Button } from "tamagui";

export default function SignInScreen() {
  return (
    <SafeAreaView>
      <Button>BIUT</Button>
    </SafeAreaView>
  );
}
